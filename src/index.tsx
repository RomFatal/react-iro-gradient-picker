import ReactGPicker from './components/Colorpicker';

// Export with new name for the react-iro-gradient-picker package
export default ReactGPicker;

// Also export with the new name as a named export
export { ReactGPicker as ReactIroGradientPicker };
